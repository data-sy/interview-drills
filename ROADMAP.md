# interview-drills 로드맵

> 이 문서가 이 레포의 **진행 상태 정본**이다. 맥락을 잊고 다시 열어도 이 파일만 읽으면 현재 상태로 복귀할 수 있게 유지한다.

## 이 레포는 무엇인가
- SI 간이테스트 자가채점 드릴 + Java 실무 레퍼런스의 **모바일 열람용** 정적 사이트.
- GitHub Pages로 배포: https://data-sy.github.io/interview-drills/
- 외부 CDN 의존 0 (구문강조 등 모든 로직 파일 내장) → 오프라인 열람 가능.

## 현재 상태 (2026-07-07 기준)
- `index.html` — 진입 페이지 (드릴 6카테고리 + Java 레퍼런스 섹션).
- 드릴 30세트: `java/sql/cs/web/csharp/oral` × `set1~5` (6카테고리 × 5세트 = 300문항).
- Java 레퍼런스 3종 (2026-07-07 이 레포에서 직접 작성 → **이미 자기완결**):
  - `java-practical-reference.html` / `-m.html` (데스크톱 / 모바일)
  - `java-codingtest-reference.html` / `-m.html`
  - `java-mybatis-jpa-reference.html` / `-m.html`
  - 모바일판(`*-m.html`)은 레포 테마(paper/brass + 다크모드) 적용, 본문·구문강조는 데스크톱판과 동일(스타일시트만 교체).

## 백로그
- [ ] **정본 데이터를 이 레포로 이관해 자기완결화 (우선순위: 높음)**
  - **문제**: 드릴 세트·`index.html` 등의 **정본은 현재 별도 비공개 repo에 있고, 이 public 레포에는 렌더 결과만 배포**된다(index footer에 "정본은 비공개 repo, 렌더 결과만 배포"로 명시됨).
  - **리스크**: 이 레포에서 직접 수정한 내용(예: 2026-07-07 `index.html`의 레퍼런스 섹션 추가)이 다음 배포 때 정본 렌더로 **덮어써질 수 있음**.
  - **목표**: 정본 데이터(드릴 문제 소스·index 생성 소스 등)를 모두 이 레포로 옮겨 **단일 소스(self-contained)** 로 통합. 이관 후 비공개 레포 → 렌더 배포 파이프라인 의존 제거.
  - **참고**: Java 레퍼런스 3종은 이 레포에서 직접 작성했으므로 이관 대상 아님(이미 정본이 여기 있음).
  - 기록일: 2026-07-07.

## 로그
- 2026-07-07 — Java 레퍼런스 3종(데스크톱 `f3c3b33` / 모바일 `d4096d9`) 추가, `index.html`에 레퍼런스 섹션 링크. ROADMAP.md 신설 + 정본 이관 백로그 등록.
